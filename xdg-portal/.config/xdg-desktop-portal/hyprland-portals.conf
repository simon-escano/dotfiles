[preferred]
# usage: interface=backend;fallback

# Default to the Hyprland backend for core Wayland features (Screen sharing, etc.)
default=hyprland

# Explicitly route all File Chooser requests to the terminal backend
org.freedesktop.impl.portal.FileChooser=termfilechooser

# Specific overrides for other portals if necessary (optional)
# org.freedesktop.impl.portal.Screenshot=hyprland
# org.freedesktop.impl.portal.ScreenCast=hyprland